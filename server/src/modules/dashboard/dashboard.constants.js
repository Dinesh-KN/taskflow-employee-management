import { TASK_STATUS } from '../tasks/task.constants.js';

export const DASHBOARD_DEFAULTS = Object.freeze({
  RECENT_LIMIT: 5,
  LIST_LIMIT: 5,
  UPCOMING_DAYS: 7,
});

export const DASHBOARD_LIMITS = Object.freeze({
  MIN_LIMIT: 1,
  MAX_RECENT_LIMIT: 20,
  MAX_LIST_LIMIT: 20,
  MIN_UPCOMING_DAYS: 1,
  MAX_UPCOMING_DAYS: 30,
});

export const ACTIVE_TASK_EXCLUDED_STATUSES = Object.freeze([
  TASK_STATUS.COMPLETED,
  TASK_STATUS.ARCHIVED,
]);

export const DASHBOARD_TASK_STATUS_KEYS = Object.freeze([
  TASK_STATUS.TODO,
  TASK_STATUS.IN_PROGRESS,
  TASK_STATUS.IN_REVIEW,
  TASK_STATUS.COMPLETED,
]);
